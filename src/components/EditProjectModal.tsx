/**
 * Substituído por `ProjectFormModal.tsx`: criar e editar passaram a usar o
 * mesmo formulário. Este arquivo ficou apenas como ponte e pode ser apagado.
 */
export { default } from './ProjectFormModal';
